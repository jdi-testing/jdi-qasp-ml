name: JDI QASP websockets version

on:
  push:
    branches: [ "fast-api-websockets" ]

jobs:
  release:
    uses: xjdi-testing/jdi-qasp-ml/.github/workflows/.release-workflow.yml@develop
    with:
      image_tag: ws
